# 🎯 벡터 유사도 Threshold 설정 가이드
# 
# 이 파일을 .env 파일에 복사하여 사용하세요.
# 값이 클수록 더 엄격한 유사도 요구 (환각 방지), 작을수록 더 많은 답변 (응답률 증가)

# 권장 설정값들:
# VECTOR_SIMILARITY_THRESHOLD=0.65   # 🎯 균형잡힌 설정 (권장)
# VECTOR_SIMILARITY_THRESHOLD=0.7    # 기존값 (안전하지만 응답률 낮음)
# VECTOR_SIMILARITY_THRESHOLD=0.6    # 관대한 설정 (응답률 높지만 환각 위험)

# 현재 권장 설정:
VECTOR_SIMILARITY_THRESHOLD=0.65

# ⚠️ 주의사항:
# - 0.5 이하: 매우 위험 (관련 없는 답변 가능성)
# - 0.75 이상: 매우 엄격 (대부분 fallback 응답)
# - 운영 환경 적용 전 반드시 테스트 필요

# 📊 Threshold별 예상 효과:
# 0.8  - 매우 엄격: 응답률 30%, 안전성 매우 높음
# 0.75 - 엄격:     응답률 45%, 안전성 높음  
# 0.7  - 기존:     응답률 60%, 안전성 높음
# 0.65 - 균형:     응답률 75%, 안전성 보통 (권장)
# 0.6  - 관대:     응답률 85%, 안전성 보통
# 0.55 - 매우관대: 응답률 90%, 안전성 낮음 